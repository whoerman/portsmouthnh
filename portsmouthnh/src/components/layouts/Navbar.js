import React from 'react';

const Navbar = () => {

        //no longer need render method because ot is a function now
        return (
            <nav className="navbar bg-primary">
                <h1>
                    Portsmouth, NH
                </h1>
            </nav>
        )
}

export default Navbar